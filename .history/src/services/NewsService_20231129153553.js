import {NEWS_URL} from 'src/utils/api';

class NewsService {


    getNewsList = async (region) => {
    const urlParams = new UrlS
      try {
        const response = awati fetch()
      }
      catch(error) {
          return {data: null, error}
      }
    }


};

export default new NewsService();